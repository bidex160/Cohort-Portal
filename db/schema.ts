import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const enrolments=sqliteTable("enrolments",{id:text("id").primaryKey(),fullName:text("full_name").notNull(),title:text("title").notNull(),hospital:text("hospital").notNull(),email:text("email").notNull(),phone:text("phone").notNull(),cohort:text("cohort").notNull(),wave:text("wave").notNull(),coordinator:text("coordinator").notNull().default(""),recordsLead:text("records_lead").notNull().default(""),ictLead:text("ict_lead").notNull().default(""),financeLead:text("finance_lead").notNull().default(""),cmacLead:text("cmac_lead").notNull().default(""),vendor:text("vendor").notNull().default(""),actions:text("actions").notNull().default(""),status:text("status").notNull().default("pending_review"),createdAt:integer("created_at",{mode:"timestamp"}).notNull()});
export const mouUploads=sqliteTable("mou_uploads",{id:text("id").primaryKey(),enrolmentId:text("enrolment_id").notNull(),objectKey:text("object_key").notNull(),fileName:text("file_name").notNull(),contentType:text("content_type").notNull(),sizeBytes:integer("size_bytes").notNull(),createdAt:integer("created_at",{mode:"timestamp"}).notNull()});
